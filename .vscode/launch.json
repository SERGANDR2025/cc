{
    "version": "0.2.0",
    "configurations": [
  
      // ✅ Упрощённый режим: только запуск (без отладки, без GDB)
      {
        "name": "▶ Run CryptoWebSocket (Release)",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceFolder}/build/CryptoWebSocket",
        "args": [],
        "stopAtEntry": false,
        "cwd": "${workspaceFolder}/build",
        "console": "integratedTerminal",
        "externalConsole": false
      }
  
      /*
  
      // 🐞 Старый режим с отладкой GDB — оставлено на всякий случай
      {
        "name": "Launch CryptoWebSocket (Release)",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceFolder}/build/CryptoWebSocket",
        "args": [],
        "stopAtEntry": false,
        "cwd": "${workspaceFolder}/build",
        "environment": [
          { "name": "LD_LIBRARY_PATH", "value": "/usr/local/lib:/usr/lib" }
        ],
        "externalConsole": false,
        "MIMode": "gdb",
        "preLaunchTask": "build-release-preset",
        "console": "integratedTerminal",
        "setupCommands": [
          {
            "description": "Enable pretty-printing for gdb",
            "text": "-enable-pretty-printing",
            "ignoreFailures": true
          }
        ]
      }З
  
      */
  З
    ]
  }
   